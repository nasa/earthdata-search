/**
 * Apollo Client names for different endpoints
 * Note: EDSC is not listed here as it is the default
 */
export const apolloClientNames = {
  CMR_ORDERING: 'CMR_ORDERING',
  CMR_GRAPHQL: 'CMR_GRAPHQL'
}
