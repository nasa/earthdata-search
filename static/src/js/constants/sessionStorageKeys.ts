/**
 * Session storage keys for the application
 */
export const sessionStorageKeys = {
  // `dontShowNlpPopup` prevents the Home NLP preference notice from reappearing
  dontShowNlpPopup: 'dontShowNlpPopup'
}
